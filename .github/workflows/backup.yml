name: Supabase Backup

on:
  schedule:
    # 3 AM y 3 PM CDMX → 9:00 y 21:00 UTC
    - cron: "0 9,21 * * *"
  workflow_dispatch:      # Permite correrlo manualmente

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Install PostgreSQL 17 client
        run: |
          # Agregar el repositorio oficial de PostgreSQL
          echo "deb http://apt.postgresql.org/pub/repos/apt/ $(lsb_release -cs)-pgdg main" | sudo tee /etc/apt/sources.list.d/pgdg.list

          # Agregar la llave del repositorio
          wget --quiet -O - https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo gpg --dearmor -o /usr/share/keyrings/pgdg.gpg

          # Actualizar lista de paquetes
          sudo apt-get update

          # Instalar PostgreSQL 17 client
          sudo apt-get install -y postgresql-client-17 sshpass

      - name: Run pg_dump using Session Pooler
        run: |
          DATE=$(date +"%Y-%m-%d_%H-%M-%S")
          FILE="supabase_backup_$DATE.sql"
          PGPASSWORD=${{ secrets.DB_PASSWORD }} pg_dump \
            -h ${{ secrets.DB_HOST }} \
            -U ${{ secrets.DB_USER }} \
            -d ${{ secrets.DB_NAME }} \
            -p 5432 \
            --no-owner --no-privileges > $FILE
          echo "Backup saved to $FILE"
          echo "BACKUP_FILE=$FILE" >> $GITHUB_ENV

      - name: Upload artifact to GitHub
        uses: actions/upload-artifact@v4
        with:
          name: supabase-backup
          path: "*.sql"
          retention-days: 30

      # - name: Copy backup to Synology NAS
      #   run: |
      #     sshpass -p "${{ secrets.NAS_PASSWORD }}" scp -o StrictHostKeyChecking=no \
      #       "$BACKUP_FILE" \
      #       ${{ secrets.NAS_USER }}@${{ secrets.NAS_HOST }}:/volume1/backups/supabase/

